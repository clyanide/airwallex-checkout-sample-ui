/* eslint-disable import/prefer-default-export */

// Dummy function for convert form country input to standardised country code
const getCountryCode = (country: string) => 'CN';

export { getCountryCode };
